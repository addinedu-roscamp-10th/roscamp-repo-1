# 기본 포트
port 6379

# 외부 접속 허용 (compose 내부 통신용)
bind 0.0.0.0

# 메모리 최대 사용량
maxmemory 256mb
maxmemory-policy allkeys-lru

# AOF 활성화 (Redis 재시작 후 큐 복구)
appendonly yes
appendfsync everysec

# 기본 DB 수
databases 4
